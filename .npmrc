# Allow build scripts for trusted packages
enable-pre-post-scripts=true

# Approve specific packages to run build scripts
# These are safe, commonly-used packages in Next.js projects
@tailwindcss:oxide=allow
bufferutil=allow
sharp=allow
utf-8-validate=allow
